server:
  port: 8080

spring:
  application:
    name: agent-gateway-java
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-turbo
          temperature: 0.7
