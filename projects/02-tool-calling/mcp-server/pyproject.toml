[project]
name = "review-mcp-server"
version = "0.1.0"
description = "MCP server for reviewing plans and documents using Qwen 3.7 Max"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
review-server = "server:main"
