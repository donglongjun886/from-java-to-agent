# 学习计划 v2 —— 面试实战对标 + 项目落地（长期版）

> 承接 v1（四周入门计划 Day1-20 已完成）。本阶段定位：**面试实战对标 + 生产级项目落地**。
>
> **这是一份长期计划，不设时间表，持续推进到拿到 offer 为止；边做边投，有一个完整项目即可开始讲、开始投。**
>
> 依据：十余份大厂 Agent 岗真实面经收敛出的考点信号 + 两轮 review。面经明细见私有面试笔记。

## 核心理念

**一个完整的 Agent 项目为主轴，源码与系统学习都回填到它，全部沉淀成「可讲口径」并落回简历。**

- 项目**核心是 Agent 能力本身**（规划 / 记忆 / 工具 / 评估 / 多 Agent 反馈），**不是后端包装**。
- 求职主线是把标签从「传统 Java」扳到「AI Agent 工程化」——所以项目要秀 Agent，不要秀工作流引擎/后端老本。
- 后端功力（并发 / 稳定 / 工程化）是**隐性内功**，让项目跑得稳、扛得住追问，但不是项目主题。
- 面试「讲两个项目」= 1 个新 Agent 项目 + 得物存量项目（冷热分离 / CQRS / 乐观锁 / 资损防控，真数字真 tradeoff，天然第二谈资）。**新项目只需 1 个。**

---

## 主轴：实战项目（1 个，vibe coding，边做边投）

### 🔲 TODO：项目选题待讨论

**选题尚未确定，下次优先讨论。** 用以下四条标准来筛：

- **核心是 Agent 能力**：能讲清规划 / 记忆 / 工具调用 / 评估 / 多 Agent，不是后端 CRUD 包装
- **有真实场景 + 真实数据**：能扛「具体数字 / 参数 / 边界」的追问（贯穿铁律）
- **vibe coding 能做完**：一个人、边做边投
- **搭业务纵深**：结合电商 / 支付 / 风控背景，做出别人做不出的差异化角度

### 项目里要埋的工程点（选题定了之后逐个落地）

- [ ] **Evaluation 内建**（重点，见下）
- [ ] 多租户 / 多用户隔离（向量库 + 上下文隔离）
- [ ] 意图澄清 / 指令模糊时的边界处理（澄清反问 / 默认值 / 降级）
- [ ] 关键参数可解释（压缩阈值 / token 归因 / 检索策略，为什么是这个值）
- [ ] 多 Agent 通信（主子 Agent 感知 / 返回 / 超时 / 重试）
- [ ] 输出风控 / 脱敏（后端风控经验迁移，轻量）
- [ ] 源码借鉴的体现（借鉴了什么、砍了什么、为什么）
- [ ] 后端内功（并发 / 稳定 / 状态持久化）——隐性支撑，不当卖点

**过关标准**：项目能独立讲透，任何「具体数字 / 参数 / 真实边界」追问都接得住。

---

## 红线知识（必考，伴随项目补）

### Evaluation 评估工程（面经出现频次最高，及格线）

- 评测集构建（query / 标准答案怎么产生、如何覆盖路径）
- 指标体系（召回 / 准确率量化，混淆矩阵 Recall / Precision / Accuracy / Specificity）
- 评测方法论（量化有效性、迭代依据、跨模型分数对齐）
- **数据飞轮**：线上 Bad Case → 清洗 → 回流 Eval 集，形成闭环
- **工程化迁移（差异化）**：把 ML 评估翻译成回归测试 / 灰度 / 版本兼容 / 幂等

**过关标准**：能为自己的项目设计一套评估方案并讲清指标与迭代闭环。

### Claude Code 内核（多家公共必考，看源码理解，回填项目）

> 源码是**看、辅助理解、回填项目**的手段，不刻意拔高优先级。读透后框架内核题自然答得上，读完可主动写进简历。

- 记忆系统：分类 / 写入 / 去重 / 冲突判断 / 衰退周期
- 上下文压缩：机制 / 触发阈值 / 信息损耗评估
- Skill 机制：注册 / 检索 / 触发率优化
- Agent Loop：防死循环 / 轮次熔断
- Prefix Caching：仅应用层视角（怎么组织 prompt 不破坏前缀缓存）

**过关标准**：能画出记忆写入 / 去重流程图，并说出 3 个设计权衡。

---

## 弱区补课（收敛范围，不发散）

- **Prompt Engineering / 结构化输出稳定性**（AI 浅区短板，高频）
- 记忆架构的价值论证（token 降了效果怎么保证）
- 上下文压缩 vs 增大长窗口的趋势判断（正交非二选一）
- 可解释性（给用户还是开发者、如何驱动优化）
- Post-training 边界话术（工程侧划清与算法团队分工）
- 安全（prompt injection / 越权 / 幻觉兜底）
- 需求研发 Agent（PRD→代码）关键要素 —— **仅作话术，不立项**

**过关标准**：每个话题能讲一段有观点、有 tradeoff 的话。

---

## 主场打磨（把优势变话术）

- **Java 手搓 Agent 框架辩护**：把「Java 转 Agent」翻成优势（工程化 / 类型安全 / 并发 / 后端融合 / 可维护性）
- **「效率提升 ≠ 效果提升」**：立项方向误判比实现层坑更致命
- **判断力来源**：踩过的坑 + 系统设计沉淀 + 业务约束权衡
- **识别边界 / 工程克制**：敢说何时「不用 RAG / 不用 Agent Team / 不用 embedding」
- **检索引擎选型**：ES / 倒排 / 与向量库如何取舍

**过关标准**：每条有一段随口能讲的标准话术。

---

## 顺带（低优先级）

### 算法保持不手生（不刻意刷，收益不大）

- 面前扫一眼常见题型（滑动窗口、海量 Top-K、字符串 / DP），保持手感即可
- 不专门刷题、不优化题型

---

## 收敛信号速查（本计划的依据）

| 类别 | 内容 |
|------|------|
| 🔴 红线必补 | Evaluation 评估工程（及格线）、Claude Code 内核（记忆/压缩/Skill/Loop） |
| 🟢 主场发力 | 后端并发/高并发调度、多 Agent 通信、检索引擎选型、多租户隔离、扩展性/成本、Java 手搓辩护、工程克制、性能 tradeoff、开发者哲学（均作项目内功 + 话术，不当项目主题） |
| 🟡 弱区待补 | Prompt 工程/结构化输出稳定性、记忆价值论证、压缩 vs 长窗口趋势、可解释性、Post-training 边界、安全 |
| ⚪ 贯穿铁律 | 「具体数字/参数/客户反馈/真实边界」的追问，只有真做过生产项目才接得住 |

> 迭代记录：定位改为长期（到 offer 为止）；聚焦 1 个 Agent 项目（项目核心是 Agent 能力，非后端包装）；项目选题记为 TODO 待讨论；工作流引擎不作项目核心（后端能力作隐性内功）；源码降回普通学习层级；算法降为顺带；Evaluation 保持红线；新增 Prompt 工程/稳定性、安全；每模块补「过关标准」。